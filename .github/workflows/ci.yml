name: CI
on:
  push:
    branches: [main]
    tags: ["*"]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    name: Julia ${{ matrix.version }} - ${{ matrix.os }} - ${{ github.event_name }}
    runs-on: ${{ matrix.os }}
    continue-on-error: ${{ matrix.version == 'nightly' }}
    strategy:
      fail-fast: false
      matrix:
        version:
          - '1.10'
          - '1'
          - 'nightly'
        os:
          - ubuntu-latest
          - windows-latest
          - macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: julia-actions/setup-julia@v2
        with:
          version: ${{ matrix.version }}
      - uses: julia-actions/cache@v2
      # TEMPORARY: AbstractStores is not in the General registry yet, so plain
      # resolution cannot find it. Delete this step once it is registered —
      # Project.toml already carries the normal [deps]/[compat] entries, and the
      # `Pkg.add` below becomes a no-op the moment the registry can serve it.
      - name: Bootstrap unregistered AbstractStores
        shell: julia --color=yes --project=. {0}
        run: |
          using Pkg
          try
              Pkg.add(name="AbstractStores", version="0.1")
          catch
              @warn "AbstractStores not in the registry yet; adding from git"
              Pkg.add(url="https://github.com/JuliaServices/AbstractStores.jl.git")
          end
      - uses: julia-actions/julia-buildpkg@v1
      - uses: julia-actions/julia-runtest@v1
      - uses: julia-actions/julia-processcoverage@v1
      - uses: codecov/codecov-action@v5
        if: ${{ matrix.version == '1' && matrix.os == 'ubuntu-latest' }}
        with:
          files: lcov.info
          token: ${{ secrets.CODECOV_TOKEN }}
  docs:
    name: Documentation
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: read
    steps:
      - uses: actions/checkout@v4
      - uses: julia-actions/setup-julia@v2
        with:
          version: '1'
      - uses: julia-actions/cache@v2
      - name: Install docs dependencies
        shell: julia --color=yes {0}
        run: |
          using Pkg
          Pkg.activate("docs")
          # TEMPORARY: mirrors the bootstrap in the test job, and must run before
          # OAuth is developed here so its AbstractStores dep resolves. Delete
          # once AbstractStores is registered in General.
          try
              Pkg.add(name="AbstractStores", version="0.1")
          catch
              @warn "AbstractStores not in the registry yet; adding from git"
              Pkg.add(url="https://github.com/JuliaServices/AbstractStores.jl.git")
          end
          Pkg.develop(PackageSpec(path=pwd()))
          Pkg.instantiate()
      - name: Build and deploy docs
        shell: julia --color=yes --project=docs {0}
        run: include(joinpath(ENV["GITHUB_WORKSPACE"], "docs", "make.jl"))
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DOCUMENTER_KEY: ${{ secrets.DOCUMENTER_KEY }}
